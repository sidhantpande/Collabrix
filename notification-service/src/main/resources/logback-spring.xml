<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="serviceName" source="spring.application.name" defaultValue="mobflow-service"/>
    <property name="CONSOLE_LOG_PATTERN" value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} level=%5p service=${serviceName} correlationId=%X{correlationId:-} method=%X{httpMethod:-} path=%X{httpPath:-} status=%X{httpStatus:-} thread=%thread logger=%logger{36} message=%msg%n%ex"/>

    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="org.mongodb.driver" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
