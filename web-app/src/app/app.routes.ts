import { Routes } from '@angular/router';
import { RegisterComponent } from './features/auth/pages/register.component';

export const routes: Routes = [{ path: 'signup', component: RegisterComponent }];
